using UnityEngine;
using System.Collections;

public class networkPlayer : Photon.PunBehaviour {

	// Use this for initialization
	void Start () {
	    
	}
	
	// Update is called once per frame
	void Update () {
//        this.GetComponentInParent<PhotonTransformView>().SetSynchronizedValues();

	}
}
